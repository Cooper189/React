import React, { Component } from 'react';

class Grid extends Component {
    render() {
        return (
            <h1>Grid{this.props.screenId}</h1>
        )
    }
}

export default Grid